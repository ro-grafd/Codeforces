#include <bits/stdc++.h>
using namespace std;
void perm( int& n, int& k)
{
    vector<int>ans;
    for(int i=1;i<=n;i++)
    {
        for(int j=i;j <= i+k-1 ; j++)
        {
            
        }
    }
}

int main()
{
    int t,n,k;
    cin>>t;
    while(t--)
    {
        cin>>n>>k;
        perm(n,k);
    }
    return 0;
}