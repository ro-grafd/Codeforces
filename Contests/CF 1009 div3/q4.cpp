#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tt;
    cin >> tt;
    while(tt--)
    {
        int n, m;
        cin >> n >> m;
        vector<int>centers(n,0);
        vector<int>radii(n,0);
        for(int i = 0 ;i <n; i++) cin >> centers[i];
        for(int i = 0; i <n; i++) cin >> radii[i];
        
        
    }
    return 0;
}